import PaginaNavigatore from '@/components/PaginaNavigatore';

export const metadata = {
  title: 'Navigatore GPS',
  description: 'Navigazione semplice per moto: mappa, prossima manovra e GPS in tempo reale.',
};

export default function NavigaPage() {
  return <PaginaNavigatore />;
}
